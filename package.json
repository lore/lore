{
  "name": "lore-monorepo",
  "private": true,
  "version": "0.0.0",
  "author": "Storcery",
  "description": "This is the monorepo for Lore (A Convention driven framework for React + Redux applications)",
  "license": "MIT",
  "main": "",
  "keywords": [],
  "files": [],
  "scripts": {
    "clean": "monorepo --baseDirectories . --removeDirectories node_modules lib coverage .tmp --command clean",
    "build": "monorepo --baseDirectories . --install",
    "build:production": "monorepo --baseDirectories . --install --production",
    "postinstall": "npm run build:production",
    "lint": "echo 'tbd: implement me.'",
    "check": "npm run lint && npm run test",
    "prepublish": "npm run clean && npm run build && npm run check",
    "test": "monorepo --baseDirectories . --test",
    "debug": "monorepo --baseDirectories . --command debug"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/storcery/lore.git"
  },
  "bugs": {
    "url": "https://github.com/storcery/lore/issues"
  },
  "homepage": "https://github.com/storcery/lore#readme",
  "dependencies": {
    "monorepo": "0.1.8"
  },
  "devDependencies": {},
  "peerDependencies": {
  }
}
