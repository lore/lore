{
  "name": "lore-hook-auth",
  "version": "0.11.0-rc.1",
  "license": "MIT",
  "main": "lib/index.js",
  "description": "A Lore hook that generates an action and reducer for retriving the current user.",
  "keywords": [
    "lore",
    "hook",
    "auth"
  ],
  "scripts": {
    "build": "../../node_modules/.bin/babel src --out-dir lib",
    "clean": "rimraf lib",
    "debug": "mocha debug --compilers js:babel-core/register --recursive",
    "prepublish": "npm run build",
    "test": "mocha --compilers js:babel-core/register test/bootstrap.js test/**/*.spec.js --recursive"
  },
  "dependencies": {
    "lore-actions": "^0.11.0-rc.1",
    "lore-utils": "^0.11.0-rc.1"
  },
  "devDependencies": {
    "chai": "3.4.1",
    "mocha": "2.3.4",
    "rimraf": "2.5.2"
  }
}
