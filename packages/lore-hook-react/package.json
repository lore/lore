{
  "name": "lore-hook-react",
  "version": "0.12.6",
  "license": "MIT",
  "main": "lib/index.js",
  "description": "A Lore hook that builds the root component and mounts it to the DOM",
  "keywords": [
    "lore",
    "hook",
    "react"
  ],
  "scripts": {
    "build": "../../node_modules/.bin/babel src --out-dir lib",
    "clean": "rimraf lib",
    "debug": "mocha debug --compilers js:babel-core/register --recursive",
    "prepublish": "npm run build",
    "test": "mocha --compilers js:babel-core/register test/**/*.spec.js --recursive"
  },
  "dependencies": {
    "react-redux": "^4.4.1"
  },
  "devDependencies": {
    "chai": "^3.4.1",
    "mocha": "^2.3.4",
    "react": "^15.4.0",
    "react-dom": "^15.4.0",
    "react-router": "^3.0.0",
    "redux": "^3.0.2"
  },
  "peerDependencies": {
    "react": "^15.4.0",
    "react-dom": "^15.4.0",
    "react-router": "^3.0.0",
    "redux": "^3.0.2"
  }
}
